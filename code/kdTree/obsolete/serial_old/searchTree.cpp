//
// Local includes
//

#include "Tree.h"


//
// Function: searchTree
//

// THIS FILE JUST CONTAINS COMMENTS FOR NOW
void Tree::searchTree(double *found, Tree tree, double *c0, double rad) {
	// rad is the circle radius
	// c0 is the center of the circle

	// get get range

	// check for intersection

	// if intersecting

		// if n > 1
			// searchTree( foundL, tree.L, c0, rad )
			// searchTree( foundR, tree.R, c0, rad )
			// found = concat( foundL, foundR )
		// else
			// found = tree.data
		// end

	// end
}
